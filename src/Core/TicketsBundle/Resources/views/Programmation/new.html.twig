{% extends "::layout.html.twig" %}

{% block body %}
<header>
  <aside>
    <ul class="breadcrumb">
      <li><a href="{{ path('admin') }}">Admin</a></li>
      <li><a href="{{ path('programmation_list') }}">Programmations</a></li>
    </ul>
  </aside>
  <h1>Ajouter une programmation</h1>
</header>

<form action="{{ path('programmation_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('programmation_list') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}