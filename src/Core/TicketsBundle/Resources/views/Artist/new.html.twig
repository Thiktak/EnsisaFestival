{% extends "::layout.html.twig" %}

{% block body %}
<h1>Artist creation</h1>

<form action="{{ path('artist_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('artist') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}