<h1>ArtistProgrammation</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Artist_id</th>
            <td>{{ entity.artistid }}</td>
        </tr>
        <tr>
            <th>Programmation_id</th>
            <td>{{ entity.programmationid }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('artistprogrammation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('artistprogrammation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('artistprogrammation_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
